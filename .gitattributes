# Normalise line endings in the repository to LF.
* text=auto eol=lf

# These MUST be LF regardless of platform. A shell script committed with CRLF
# fails on Linux with "bad interpreter: No such file or directory", which is a
# confusing way to discover a line-ending problem mid-deploy.
*.sh text eol=lf
Dockerfile text eol=lf
